# Adresse de l'API de données (projet portfolio-api).
# Copier ce fichier en .env pour la changer. C'est le navigateur du visiteur
# qui appelle l'API : en production, mettre son URL publique.
VITE_API_URL=http://localhost:3000
